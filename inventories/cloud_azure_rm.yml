plugin: azure.azcollection.azure_rm
# fetches VMs from an explicit list of resource groups instead of default all (- '*')
# include_vm_resource_groups:
#     - "{{ lookup('env','RESOURCEGROUP')}}"
hostnames:
  - name

conditional_groups:
  linux: true
  database: "'database' in tags.demo_role"
